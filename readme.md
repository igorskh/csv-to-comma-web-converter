# Simple Flask Web CSV Converter
This web app is very basic service that allows to upload semicolon-separated file and comma-separated with quatation marks for every entry.

## Run locally
With virtual environment:
```bash
virtualenv venv
source venv/bin/activate
pip install -r requirements.txt
```

Then run `python index.py`

## Deploy with Zeit
Just run `now` to deploy.